# The Portfolio Dashboard used to be served statically from /portfolio-dashboard/.
# It now runs as its own Cloudflare Worker behind an access code, so the case data
# is no longer published here. This redirect covers both hnguyen.dev and the
# *.pages.dev domain, since it is applied by Pages itself rather than by a zone rule.
/portfolio-dashboard/* https://portfolio-simplification-analytics.huy1999nguyen.workers.dev/ 302
