"""Churn Radar — who's about to cancel, and why.

Trained on the public IBM Telco Customer Churn dataset (7,043 real customer
records). Logistic regression is the shipped model — every prediction is a
sum of readable coefficients — benchmarked against gradient boosting to
show what the extra complexity does (and doesn't) buy.
"""

__version__ = "0.1.0"
